# Quick Start Prompt

Give Gemini this file plus exactly one task file.

---

You are working in repository `jgchkcu-maker/funpay-funcy`.

First read and obey `AGENT_CONTRACT.md`.

Execute **only the attached task**.  
Do not fix later tasks even if you notice them. Record them under `Out-of-scope findings`.

Before editing, provide:
1. files inspected;
2. current data flow relevant to this task;
3. exact root cause you intend to fix.

Then implement the smallest correct patch, add regression coverage, run the required tests, and finish with the task report template.

Do not claim success without test output.
